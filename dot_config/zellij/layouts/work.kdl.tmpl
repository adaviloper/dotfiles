layout {
  cwd "{{ .chezmoi.homeDir }}/Code/suralink/"

  default_tab_template {
    pane size=1 borderless=true {
      plugin location="zellij:tab-bar"
    }
    children
    pane size=2 borderless=true {
      plugin location="zellij:status-bar"
    }
  }

  tab name="OcrOrchestrator" cwd="{{ .chezmoi.homeDir }}/Code/suralink/ocr_orchestrator_service" {
    pane
  }

  tab name="OcrTextract" cwd="{{ .chezmoi.homeDir }}/Code/suralink/ocr_textract" {
    pane
  }

  tab name="SLAPI" cwd="{{ .chezmoi.homeDir }}/Code/suralink/slapi/server" {
    pane
  }

  tab name="Files Service" cwd="{{ .chezmoi.homeDir }}/Code/suralink/files_service" {
    pane
  }

  tab name="Terminals" {
    pane split_direction="vertical" {
      pane
      pane split_direction="horizontal" {
        pane
        pane
      }
    }
  }
}
