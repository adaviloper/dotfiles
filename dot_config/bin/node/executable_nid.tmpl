#!/bin/bash

{{ template "shared_pick_node_package_manager.tmpl" . -}}

case "$(_pick_node_package_manager)" in
  yarn) yarn add --dev "$@" ;;
  pnpm) pnpm  add -D "$@" ;;
  npm) npm  install -D "$@" ;;
esac
