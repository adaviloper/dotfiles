#!/bin/bash

{{ template "shared_pick_node_package_manager.tmpl" . -}}

case "$(_pick_node_package_manager)" in
  yarn)
    if [ $# -gt 0 ]; then
      yarn add "$@"
    else
      yarn install
    fi
    ;;
  pnpm) pnpm install "$@" ;;
  npm) npm install "$@" ;;
esac
